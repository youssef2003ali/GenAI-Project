[project]
name = "acs-shared"
version = "0.1.0"
description = "Shared schemas, models, and base agent for ACS"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "httpx>=0.27",
]

[dependency-groups]
dev = ["pytest>=8.0"]
