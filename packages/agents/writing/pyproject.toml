[project]
name = "acs-agent-writing"
version = "0.1.0"
description = "Writing Agent - full content generation from outline"
requires-python = ">=3.11"
dependencies = ["acs-shared"]

[dependency-groups]
dev = ["pytest>=8.0"]
