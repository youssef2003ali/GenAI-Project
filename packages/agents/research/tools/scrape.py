"""Scrapling MCP wrapper. Phase 1: dummy implementation."""


async def scrape_content(url: str) -> str:
    """Extract clean content from a URL. Phase 1: returns dummy text."""
    return f'Extracted content from {url}: This is simulated extracted content for Phase 1 skeleton testing. In Phase 2, Scrapling will extract real content from the page.'
